# Architecture Decision Records

One file per major decision, e.g. `0001-no-vector-database.md`. Record the decision, the
context that forced it, and the alternatives rejected -- this is direct viva prep material
(see `CLAUDE.md` coding standards).
